[build]
  # This tells Netlify where to find your serverless functions
  functions = "netlify/functions"

# Optional: Create a clean API route
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
